"use client";

const AddCart = () => {
  return (
    <div>
      <button
        className="btn btn-primary"
        onClick={() => console.log("clicked")}
      >
        AddToCart
      </button>
    </div>
  );
};

export default AddCart;
